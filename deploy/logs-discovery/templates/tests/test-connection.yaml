apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logs-discovery.fullname" . }}-test-connection"
  labels:
    {{- include "logs-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logs-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
