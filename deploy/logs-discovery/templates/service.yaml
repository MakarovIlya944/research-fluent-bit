apiVersion: v1
kind: Service
metadata:
  name: {{ include "logs-discovery.fullname" . }}
  labels:
    {{- include "logs-discovery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logs-discovery.selectorLabels" . | nindent 4 }}
